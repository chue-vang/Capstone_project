@model HersFlowers.Models.ViewModels.ShoppingCartFlowerViewModel
@{
    ViewData["Title"] = "TestProduct";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-md-4">
    <form asp-action="AddToCart" method="post">
        @*<input type="hidden" asp-for="Id" />*@
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">
                        Size of bouquets
                    </th>
                    <th scope="col">
                        Price
                    </th>
                    <th scope="col">
                        Quantity
                    </th>
                    @*<th scope="col">
                        Total
                    </th>*@
                </tr>
            </thead>
            <tbody>
                <tr scope="row">
                    <td>
                         Large Bouquet
                    </td>
                    <td>
                        $15
                    </td>
                    @*<td>
                        <input type="text" asp-for="Quantity" class="control-label" placeholder="Enter Quantity" />
                        <span asp-validation-for="Quantity" class="text-danger"></span>
                    </td>
                </tr>

                <tr scope="row">
                    <td>
                        Small Bouquet
                    </td>
                    <td>
                        $10
                    </td>
                    <td>
                        <input type="text" asp-for="Quantity" class="control-label" placeholder="Enter Quantity" />
                        <span asp-validation-for="Quantity" class="text-danger"></span>
                    </td>
                </tr>*@
            @*@foreach(var item in Model.Flowers)
            {
                @using{
                        ShoppingCartItem ShoppingCartItem = new ShoppingCartItem()
                        {
                            Quantity = 10,
                            FlowerId = item.Id
                        };
                    Model.ShoppingCartItems.Add(ShoppingCartItem);
                }
            }*@
                <tr>
                    <td>
                        <input type="submit" value="Checkout" />
                    </td>
                </tr>
            </tbody>
        </table>
    </form>

</div>